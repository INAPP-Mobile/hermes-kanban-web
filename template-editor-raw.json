{
    "PORT": {
        "value": "8502",
        "description": "HTTP port Railway routes to. Railway injects this automatically."
    },
    "HERMES_HOME": {
        "value": "/opt/data",
        "description": "Root directory for all Hermes persistent state (boards, profiles, config). Mounted as a Railway volume."
    },
    "HERMES_KANBAN_API_TOKEN": {
        "value": "",
        "description": "OPTIONAL bearer token. When set, all API requests (including the SSE stream) require 'Authorization: Bearer ***'. Leave empty to keep the board open."
    },
    "OLLAMA_BASE_URL": {
        "value": "${{Ollama.OLLAMA_BASE_URL}}",
        "description": "Ollama API base URL. Auto-linked to the sibling Ollama service for local LLM inference."
    }
}