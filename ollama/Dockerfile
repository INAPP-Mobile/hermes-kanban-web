FROM ollama/ollama:latest

# Companion Ollama service for hermes-kanban-web.
# The image auto-starts `ollama serve` on 0.0.0.0:11434.
# Models are persisted in the Railway volume mounted at /root/.ollama.

EXPOSE 11434